# Subrata Pramanik — GitHub Pages site

Files:
- index.html, about.html, publications.html, projects.html, resume.html, gallery.html, meeting.html
- style.css
- script.js

Images:
- profile.jpg (upload to repo root)
- bg_beach.jpg (optional local background) — this setup also uses /mnt/data/... as included if needed
- Gallery: create `/images/` folder and upload `1.jpg`, `2.jpg`, ...

To update:
- Edit HTML files in the repo; commit changes.
- Upload images to `/images/` to appear in gallery.

Visitor counter uses countapi.xyz (no account needed).
Weather uses open-meteo (no API key).
